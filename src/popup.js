// This script should be able to pick up the values from the popup and use the user options to apply filters

// let blur_checkbox = document.getElementById('checkboxes-0');
// let replace_checkbox = document.getElementById('checkboxes-1');
// let watermark_checkbox = document.getElementById('checkboxes-2');

// blur_checkbox.value = chrome.storage.sync.get('blur');
// replace_checkbox.value = chrome.storage.sync.get('replace');
// watermark_checkbox.value = chrome.storage.sync.get('watermark');

// blur_checkbox.onclick.chrome.storage.sync.set({ blur: blur_checkbox.checked });
// replace_checkbox.onclick.chrome.storage.sync.set({ replace: replace_checkbox.checked });
// watermark_checkbox.onclick.chrome.storage.sync.set({ watermark: watermark_checkbox.checked });